# Process audio entirely on device

Audio captured for tuning, along with derived pitch measurements, remains on the player's device and is never sent to a server. This preserves a simple privacy promise and avoids a backend, while accepting that detection quality will vary across browsers and devices; any future cloud processing must explicitly supersede this decision and obtain informed user consent.
